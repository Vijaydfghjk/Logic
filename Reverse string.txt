package main

import "fmt"

func Reverse(a string) string {

	rev := ""

	for i := len(a) - 1; i >= 0; i-- {

		rev += string(a[i])
	}
	return rev
}
func main() {

	R1 := Reverse("Ram")
	R2 := Reverse("Vijay")
	fmt.Printf("%s\n", R1)
	fmt.Printf("%s\n", R2)

}
